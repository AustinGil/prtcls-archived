{
  "name": "prtcls",
  "version": "0.0.10",
  "main": "src/main.scss",
  "files": [
    "src",
    "dist"
  ],
  "author": "",
  "repository": {
    "type": "git",
    "url": "git://github.com/Stegosource/prtcls.git"
  },
  "scripts": {
    "build": "rm -rf dist && parcel build src/main.scss --no-content-hash",
    "publish": "release-it",
    "p": "parcel build --no-cache src/plugin.css"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "release-it": "^13.5.8",
    "sass": "^1.26.5"
  }
}
